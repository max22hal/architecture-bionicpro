# architecture-bionicpro
API сервис написан на Spring. Код лежит в папке API. Секьюрити настроена в классе SecurityConfig, сам API сделан в класе firstController.

Для запуска выполните 

```bash
docker-compose up --build
```